export interface ILink {
    url?: string | null;
    title?: string;
    outer?: boolean;
    showOuterIcon?: boolean;
    showIconPermanently?: boolean;
    iconLeft?: boolean;
    light?: boolean;
    visited?: boolean;
}
