export interface IBiMenu {
    stepId: number;
    biId: number;
    index: number;
    totalLength: number;

    // removeBi: (id: number) => void;
    // moveBi: (index: number, up: boolean) => void;
}
