import { GRID_CELL } from './grid';

export const CLUSTER_MEMBER_W = 120;
export const CLUSTER_MEMBER_H = 48;
export const CLUSTER_HEADER_H = 24;
export const CLUSTER_MEMBER_GAP = Math.max(8, Math.floor(GRID_CELL * 0.5));
export const CLUSTER_BOX_PADDING = CLUSTER_MEMBER_GAP;

export const CLUSTER_MIN_SIZE = 5;
