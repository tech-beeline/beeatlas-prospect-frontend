import styled from '@emotion/styled';

export const LeftPanelRestoreHit = styled.div`
    position: absolute;
    top: 20%;
    left: 0;
    z-index: 10;
    transform: translateY(-50%);
    line-height: 0;
    pointer-events: auto;
`;

export const RightPanelRestoreHit = styled.div`
    position: absolute;
    top: 20%;
    right: 0;
    z-index: 10;
    transform: translateY(-50%);
    line-height: 0;
    pointer-events: auto;
`;
